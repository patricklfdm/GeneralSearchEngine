# V4.3 Phase 1 logical derived-state fixtures

These canonical JSON fixtures freeze independent classifications and the four built-in
logical component kinds before any production `(1,2)` bytes exist. They deliberately
contain `PHASE2_PENDING` instead of fabricated catalog or component magic.

`fixture-checksums.sha256` binds the complete two-file fixture inventory. Production
metadata, catalog, component, checksum and backup encodings remain Phase 2 work.
